class obstacle{
    constructor(x, y, height, width) {
        //xy = lower left corner of obstacle
        this.x = x;
        this.y = y;
        this.height = height;
        this.width = width;
    }

    isColliding(player) {
    }
}